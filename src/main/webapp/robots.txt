User-agent: *
Allow: /

# Login uses noindex + canonical in login.html (and X-Robots-Tag on /login).
# Keep Allow so Google can crawl and honor those tags.

# SPA fallback used to serve the editor at this junk path (GSC: crawled, not indexed).
Disallow: /javascript
Disallow: /javascript/

