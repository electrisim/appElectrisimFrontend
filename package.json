{
  "scripts": {
    "prepare-deploy": "node scripts/prepare-deploy.js",
    "deploy:dev": "cross-env NODE_ENV=development npm run prepare-deploy",
    "deploy:prod": "cross-env NODE_ENV=production npm run prepare-deploy",
    "minify": "node scripts/minify-js.js",
    "minify:restore": "node scripts/minify-js.js --restore",
    "minify:clean": "node scripts/minify-js.js --delete-backups"
  },
  "dependencies": {
    "nodemailer": "^7.0.3"
  },
  "devDependencies": {
    "terser": "^5.36.0"
  }
}
